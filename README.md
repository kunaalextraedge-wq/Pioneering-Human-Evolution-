Drop your two photos in this folder with these exact filenames:

- `body-hologram.jpg` — used in the homepage hero
- `brain-hologram.jpg` — used on the Hypnotherapy card and booking screen

Any JPG or PNG works. For best results use images at least 600px wide,
roughly portrait/square in shape. The site references these two filenames
directly — as long as the names match, nothing else needs to change.

(Why you're adding these yourself: they look like licensed stock photos,
so it isn't something I can copy in for you — but since you already have
the files, dropping them in here takes two seconds.)
